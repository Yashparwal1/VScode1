#include<stdio.h>
#include<stdlib.h>
int main()
{
    char str[100], c, i;
    FILE *fp;
    if (fp == NULL)
    {
        printf("Error: File couldn't opened");
        exit(0);
    }
    else
    {
        
    }
    
    
    return 0;
}