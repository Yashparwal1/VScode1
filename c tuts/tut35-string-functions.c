/* 
strcat() -- used to concatenate or combine two given strings
strlen() -- used to show length of a string
strrev() -- used to show reverse of a string ***** eg. strcpy(s2, s1) -- copy s1 into s2  
strcpy() -- used to copy one string into another
strcmp() -- used to compare two given strings ***** gives 0, +ve, -ve according to the unmaching char. of the string. 
 */

#include <stdio.h>

#include <string.h>
int main()
{
    // char str1[] = "yash";
    // char str2[] = "parwal";
    // char c[100];
    // puts(strcat(str1, str2));
    // printf("The length of str1 is = %d\n",strlen(str1));
    // printf("The length of str2 is = %d\n",strlen(str2));
    // printf("\nThe reversed string str1 is = %s",strrev(str2));
    // puts(strrev(str1));

    
    // strrev(str1);
    // puts(str1);
    // printf("reversed string str1 is %s",str1);

    // strcpy(c, str1);
    // puts(c);
    // strcpy(c, strcat(str1,str2));
    // puts(c);

    // printf("the strcmp for str1 and str2 returned %d", strcmp(str1,str2)); // str1 - str2
    

    // char s1[100], s2[100], msg[100];
    // gets(s1);
    // gets(msg);
    // gets(s2);

    // strcat(s1, msg);
    // strcat(s1, s2);

    // puts(s1);
    return 0;
}