#include<stdio.h>
// Typecasting refers to changing a variable of one datatype into another
int main()
{
    int a;
    float b=(float)54/5; // arithmatic op. between to integers always gives integer. 
    printf("a = %f",(float)b);
    return 0;
}
